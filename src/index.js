import React from 'react';
import ReactDOM from 'react-dom';
import Index from '../src/assets/js/app';


ReactDOM.render(
  <React.StrictMode>
    <Index />
  </React.StrictMode>,
  document.getElementById('root')
);